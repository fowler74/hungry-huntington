<!DOCTYPE html>
<html>
    <head>
        <title>Admin</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/assets/css/admin.css" type="text/css">
    </head>
<body>
    <hgroup>
        <h1>Admin Hungry Huntington</h1>
    </hgroup>
    {% if controller.loggedIn == true %}
    <hgroup>
        <h3>Hello, {{ controller.username|capitalize }}</h3>
        <form action="" method="POST" class="none">
            <input type="hidden" name="action" value="logout">
            <button type="submit">logout</button>
        </form>
    </hgroup>
        {% include 'inc/admin/adddeal.twig' %}
        {% include 'inc/admin/addcompany.twig' %}
        {% include 'inc/admin/deldeal.twig' %}
        {% include 'inc/admin/adduser.twig' %}
        {% include 'inc/admin/sendemail.twig' %}
    {% else %}
        {% include 'inc/admin/login.twig' %}
    {% endif %}
</body>
</html>
